@model List<PorfolioViewModel>

@{
    ViewBag.Title = "Admin | Portfolyolar";
}

<main id="main" class="main" style="min-height: 550px;">

    <div class="pagetitle">
        <h1>Portfolyolar</h1>
        <nav>
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a asp-area="Admin" asp-controller="Home" asp-action="Index">Anasayfa</a></li>
                <li class="breadcrumb-item active">Portfolyolar</li>
            </ol>
        </nav>
    </div><!-- End Page Title -->

    <section class="section">
        <div class="row">
            <div class="col-lg-12">

                <div class="card">
                    <div class="card-body">
                        <a asp-area="Admin" asp-controller="Portfolio" asp-action="Add" class="btn btn-primary text-center mt-3">Portfolyo Ekle <i class="bi bi-plus-circle"></i> </a>

                        <div class="d-flex justify-content-between align-items-center">
                            <h5 class="card-title">Tüm Portfolyolar</h5>
                        </div>

                        <!-- Table with stripped rows -->
                        <table class="table table-striped table-hover">
                            <thead>
                                <tr>
                                    <th scope="col">Resim</th>
                                    <th scope="col">Başlık</th>
                                    <th scope="col">Açıklama</th>
                                    <th scope="col">Oluşturulma Tarihi</th>
                                    <th scope="col">İşlemler</th>

                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var portfolio in Model)
                                {
                                    <tr class="align-middle">
                                        <td scope="row" class="" style="">
                                            <img src="~/images/@portfolio.Image.FileName" class="" alt="@portfolio.Title" style="width:40px; height:40px;" />
                                        </td>
                                        <td scope="row">@portfolio.Title</td>
                                        <td scope="row">@portfolio.Description</td>
                                        <td scope="row">@portfolio.CreatedDate</td>
                                        <td scope="row">
                                            <a asp-area="Admin" asp-controller="Portfolio" asp-action="Update" asp-route-portfolioId="@portfolio.Id" class="btn btn-success">Güncelle</a>
                                            <a asp-area="Admin" asp-controller="Portfolio" asp-action="Delete" asp-route-portfolioId="@portfolio.Id" class="btn btn-danger">Sil</a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                        <!-- End Table with stripped rows -->

                    </div>
                </div>

            </div>
        </div>
    </section>

</main>


