@model List<EducationListViewModel>
@{
    ViewBag.Title = "Admin | Eğitimler";
}
<main id="main" class="main" style="min-height: 550px;">

    <div class="pagetitle">
        <h1>Eğitim Güncelle</h1>
        <nav>
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a asp-area="Admin" asp-controller="Home" asp-action="Index">Anasayfa</a></li>
                <li class="breadcrumb-item active">Eğitimler</li>
            </ol>
        </nav>
    </div><!-- End Page Title -->

    <section class="section">
        <div class="row">
            <div class="col-lg-12">

                <div class="card">
                    <div class="card-body">
                        <a asp-area="Admin" asp-controller="Education" asp-action="Add" class="btn btn-primary text-center mt-3">Eğitim Ekle <i class="bi bi-plus-circle"></i> </a>

                        <div class="d-flex justify-content-between align-items-center">
                            <h5 class="card-title">Tüm Eğitimler</h5>
                        </div>

                        <!-- Table with stripped rows -->
                        <table class="table table-striped table-hover">
                            <thead>
                                <tr>
                                    <th scope="col">Başlık</th>
                                    <th scope="col">Yıl</th>
                                    <th scope="col">Eğitim Yeri</th>
                                    <th scope="col">Oluşturulma Tarihi</th>
                                    <th scope="col">İşlemler</th>

                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var education in Model)
                                {
                                    <tr class="align-middle">
                                        <td scope="row">@education.Title</td>
                                        <td scope="row">@education.Year</td>
                                        <td class="text-truncate" style="max-width:200px;" scope="row">@education.Address</td>
                                        <td scope="row">@education.CreatedDate</td>
                                        <td scope="row">
                                            <a asp-area="Admin" asp-controller="Education" asp-action="Update" asp-route-educationId="@education.Id" class="btn btn-success">Güncelle</a>
                                            <a asp-area="Admin" asp-controller="Education" asp-action="Delete" asp-route-educationId="@education.Id" class="btn btn-danger">Sil</a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                        <!-- End Table with stripped rows -->
                    </div>
                </div>

            </div>
        </div>
    </section>

</main>


